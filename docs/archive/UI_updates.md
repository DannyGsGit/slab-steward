# Updates

1. Use a sidebar and move account, map controls, trail list, trail details/value setting, changeset staging into the sidebar. Consistent with the full app
2. For ebikes, when "allowed", bring add an "up to" class selector. The classes shown should be localized based on the trail location, e.g. US gets Class 1-3, EU gets pedelec, s-pedelec, etc. For now, only go up to class 1/pedelec.
3. Trail display controls need overhaul. Instead of all/mtb/hiking, add multi-selector of parameters to provide more grain, including being able to toggle `paved` and `footway` trails.